test: sales.buyflow.mock.validate_mock201_kafka_mock404_45
description: Validating mock201_kafka_mock404 behaviors at scale
config:
  virtual_users: 1
  mock_strategy: required
scenario:
- name: step_0
  protocol: kafka
  mock:
    response:
      status: 201
      latency: 50ms
  topic: events.27
- name: step_1
  protocol: kafka
  mock:
    response:
      status: 404
      latency: ${$random.int(10,100)}ms
  topic: events.10
