test: analytics.tracking.mock.validate_kafka_mock400_65
description: Validating kafka_mock400 behaviors at scale
config:
  virtual_users: 1
  mock_strategy: required
scenario:
- name: step_0
  protocol: kafka
  mock:
    response:
      status: 400
      latency: 100ms
  topic: events.98
